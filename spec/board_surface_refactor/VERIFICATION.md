# Implementation verification

The rules-v5 inspection follow-up is tracked in [FEEDBACK_V5.md](FEEDBACK_V5.md) and [VERIFICATION_V5.md](VERIFICATION_V5.md). The receipts below describe the previously reviewed v4 implementation.

## Historical v4 verification

The integrated Godot suite passed on September 6, 2026, after the movement-presentation, relic and independent release-review fixes. Subsequent renderer-only refinements passed fresh focused and native checks. Reproduction commands and scope are in [IMPLEMENTATION.md](IMPLEMENTATION.md); the exact reviewed commit is recorded in the task handoff.

All local evidence paths below are relative to `output/board-surface-refactor/` in the task worktree. Generated logs, images and isolated saves are intentionally excluded from the game commit.

| Proof | Result and local evidence |
| --- | --- |
| Integrated regression | `full-suite-material-tracker-final.log`: `TEST RESULT: PASS`, exit 0. Includes the material and action-strip changes; final renderer precision refinements receive additional focused/native verification. Includes core surfaces, native Chain, movement, the independent core regressions, eleven enemy acceptance fixtures, relic transformations and existing game suites. `full-suite-privacy-portable-final.log`, `full-suite-reviewed-final.log` and `full-suite-verified.log` preserve earlier passes. |
| Late relic regressions | `late-relic-fixes-test.log`: relic/core/independent review wrappers pass. Covers native Faultline primary legality, event-time Black Sun underlay and surviving-source transport, including valid alternatives. |
| Presentation and analytics | `presentation-tracker-replay-final.log`: pass against a constructed game UI. Covers board cancellation, conditional Detonate values, player/illusion HP, defense-only and lethal exposure, safe/placement negatives, hidden versus revealed movement/AOE traps, known shared blasts under Eclipse, and automatic follow-ups without synthetic victory. Source attribution, append-only resume deduplication and movement feedback are also exercised. The action strip also covers active/inactive damage modifiers, skipped and sequential bonuses, completed versus current resolution values and hidden-trap threshold crossings. The 35 outcome/marker-isolation cases are retained in the presentation suite; `PRIVACY_FOLLOWUP_REVIEW.md` records their independent reproduction. |
| Hazard-aware pursuit | `ai-route-survival-focused-03.log` and `ai-dynamic-budget-focused-final.log`: pass. Safe same-turn melee detour, health and defense-aware routes, unavoidable hazards, finite-cost healthy shortcut, immutable forecast and one Fire packet per large-footprint entry. Dynamic trap-painted Rubble uses the actual movement allowance, with sufficient/insufficient-budget and first-step exception regressions. The integrated enemy-pathfinding suite now checks actual safe detour and attack outcome instead of the superseded minimum-step-first policy. |
| Migration | `save-migration-release.log`, `parent-review-release.log`: pass. Immutable conversion, actual paid refunds, resource/action-boundary preservation, guided-opening compatibility, byte-exact durable archives and archive-conflict refusal. |
| Complete content | `final-content-review/` and the data/smoke wrappers: 159 cards, 42 equipment grants, 60 relics, 30 skill definitions and 18 enemies reviewed. Data validation passes 26,694 checks; 249 card actions and 62 enemy intents match traced and ordinary resolution. |
| Python and art checks | Seven heuristic tests, five icon-policy tests, nine fixture tests and all 26 reproducible artwork imports pass. Changed GDScript declaration-block audit (including multiline initializer bodies and new files) and `git diff --check` pass. Both migrated performance scripts also pass direct script parser checks. |
| Native rendering | `visual-tracker-material-final.json`, `final-tracker-ui/`: refreshed 22-scene real-renderer probe at 1920×1080, 100% UI scale. Inspected the revised materials with conditional Detonate, player/illusion defense and lethal warnings above the actor HUD, targeting, cancellation and modified action-strip values matching the card. The full material motion/cache proof is recorded in [PRESENTATION.md](PRESENTATION.md). |
| Playable opening | `inspection-final-motion-proof.json`, `final-furnished-motion/`: final-renderer refresh of five inspected scenes from a private copy of the verified save, exercising the seven-card opening, Push, ordinary Lightning conduction, terrain ability choice and cancellation in a furnished room. An additional 96 native frames cover four seconds of procedural ground motion; sampled frames retain correct actor/prop depth, stable Ice/Rubble and changing Fire/Electrical forms. All 101 images pass at 1920×1080 with zero engine errors. `furnished-surfaces.mp4` is the encoded inspection clip. The final handoff regenerates and independently verifies the pre-action save for the reviewed commit. |
| Acquisition and enemy art | `reward-context-proof.json`, `shop-context-proof-02.json`, `loadout-context-proof-02.json`, `skill-tree-proof-02.json`, `shale-art-proof-03.json`: inspected current card, relic, equipment, skill and enemy identity in their real UI contexts. |
| Final procedural renderer | `material-presentation-final.log`: focused suite pass on the final retained-mesh source. `combined-one-frame-cache-final-01.json` and its pixel receipt compare the very next rendered frame at each supplied clock: all four normal/reduced pairs pass the unchanged 4×MSAA guard, with maximum error 4/255 and 8–10 pixels above 2/255. Twelve lifecycle cycles restore the same 2,357 board-subtree nodes; same-phase Ice width/seed changes also pass. The separate particle helper/shader review found no remaining issue. |
| Dense rendering | `combined-uniform-dense-final-01.json`: the final exact-fidelity renderer, with all 72 elemental tiles, 36 Rubble layers, five actors and the full HUD, measures normal mean 17.979 ms, upper median 25.687 ms, p95 26.965 ms, maximum 27.887 ms; no interval exceeds 33.33 ms. Reduced motion measures median 8.325 ms and p95 9.424 ms. This is a measured crowded-board cost, not a 60 fps guarantee. Earlier 8.223 ms Sprite-based results are intermediate diagnostics; they are not the final renderer. Reproduction and workload limits are recorded in [PRESENTATION.md](PRESENTATION.md). |
| Lighter-density rendering | `material-24-tile-timing-final-01.json`: the same harness with 24 elemental tiles (eight of each), 12 Rubble layers, five actors and the full HUD measures normal mean 8.334 ms, median 8.240 ms, p95 10.388 ms and maximum 14.148 ms. No interval exceeds 16.67 ms in either 180-sample normal/reduced window. This separate synthetic workload establishes how the final renderer behaves with less ground coverage; it does not replace the 72-tile comparison or certify every combat. |
| Controlled playtests | [PLAYTESTS.md](PLAYTESTS.md), `ai-dynamic-budget-playtest-refresh/` (current) and `ai-survival-playtest-refresh/` (prior comparison): six fixed-loadout builds finish with 19 combat wins over 95 activations. Three changed builds repeat their complete gameplay summaries and stable analytics fields exactly; restoring only the old attack-route candidate methods reproduces their old outcomes. The recorded 54-command mixed run still clears three combats and retreats, matching all 25 card payloads and 133 stable events. The dynamic-budget correction repeats all six builds and the manual run with identical summaries and all 1,057 stable gameplay events. These are execution and interaction checks, not comparative win-rate or full human campaign balance proof. |

The refreshed controlled runs include all late relic and AI fixes; source fingerprints remain stable through their execution. Native acquisition scenes predate the procedural floor revision and isolated combat-rule corrections; their art and layout are unchanged. The fresh material, furnished opening and release-review UI scenes cover the revised floor renderer. Fixture Python checks also pass after limiting the interactive launch's lifetime to when the player closes it, while generation and verification retain bounded timeouts.

The headless run retains the host CA-certificate diagnostic and shutdown renderer/ObjectDB warning categories reproduced by the passing exact base-master suite (`full-suite-base.log`). The deliberately ambiguous historical-save test also emits its expected preservation warning. These logs do not establish clean engine shutdown, Windows runtime certification or a complete human boss-campaign playthrough.
