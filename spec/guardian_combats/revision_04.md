# Guardian inspection revision 4

The changed surface is the combat board and its immediate targeting feedback. Keep foreground cover legible while exposing actors behind it; ground selection and movement paths must remain legible above surfaces without drawing through upright props. Watch braziers should sit at the tile center, use the existing fire visual language, and show the precise event that restores their light. Map hover describes the encounter without advertising its trophy.

Combat intent and resolution share live rules. Occupying a summon reservation redirects backup to a valid nearby square while retaining helper caps. Displacement or interrupted movement must leave a comprehensible, accurate pattern preview and matching resolution; no invisible attacks or misleading old-origin patterns. Detonation presents a ground explosion with the consumed surface’s elemental identity.

Investigate the played Storm Cantor encounter save and the reported Sweep selection lockup. Preserve raw evidence outside source control, reproduce the failure at the UI input boundary, and fix its cause. Add only pertinent explanatory text, no advice or restated core rules. Preserve pointer and controller selection/cancel paths and ordinary combat behavior.

Proof: focused resolver and real RunScene regressions; full Godot suite; inspected native 1920×1080 at 100% UI scale, normal/reduced visual sequences; persisted inspection fixtures; independent signoff on the exact committed HEAD. Publication awaits explicit user approval.

The saved Storm Cantor encounter retained an active tutorial profile outside an authored tutorial combat. Those stale tutorial flags incorrectly restricted secondary controls. The combat lockup itself reproduced when Low Sweep targeted a replacement Wisp on a defeated Wisp’s square: the visibility lookup returned the dead actor first, leaving the movement committed with no legal follow-up target. Targeting now ignores defeated occupants; interrupted shortcut attacks finish as a miss without another target request. Runtime tutorial restrictions now require an authored combat; Guardian fixture profiles also dismiss the guide. Directional patterns retain orientation after displacement and use the actual arrival tile. Summons prefer their valid reservation, then choose a nearby unoccupied square without exceeding helper caps.

Verification completed before peer review:

- Full Godot suite: `tests/run_tests.gd` via the task runner, PASS (`/private/tmp/guardian-revision-04-full-03.log`). Includes ordinary targeting/controller/cancel, the replacement-Wisp Sweep regression, interrupted-route completion, tutorial, surfaces, and all Guardian contracts. Existing ambiguous-save migration and exit ObjectDB warnings remain.
- Focused Guardian resolver/relic suite: `tests/guardian_contract_probe.gd`, PASS (`/private/tmp/guardian-revision-04-contract-final.log`). Covers each Guardian's occupied summon fallback, preview/resolution agreement after displacement, helper caps, and blocked/lethal approaches.
- Native Metal renderer: `tests/guardian_revision_04_probe.gd` through `visual_probe_runner.py`, 25 accepted 1920×1080 captures at UI scale 1.0, normal/reduced motion. Manifest: `output/guardian-revision-04/native-proof-03.json`; copied images: `output/guardian-revision-04/native/`. Inspected outcrop/actor overlap, Ice/Rubble arrow layering, both displaced patterns, relocated green summon tile, centered flames and Last Procession relight, full Fire burst, map tooltip, and completed Sweep/visible hand. Reduced run uses controller card selection and targeting; normal uses pointer. Sound trace verifies one elemental Fire cue and no default ranged cue.
- Flame frame comparison shows normal animation, while reduced motion uses a fixed phase (ambient scene glow may still change). No cutout rig or paint changed in this pass.
- Python balance surface/progression tests and icon identity policy: PASS. Updated `balance_evidence.json` with current assumptions and actual scorer results; no intrinsic card coefficients changed. Previous pacing trials remain labeled historical.

The actual played Cantor snapshot and logs are preserved locally in `output/guardian-revision-04/user-evidence/`. Its Low Sweep replay now finishes against the living Wisp and releases selection/animation/commit locks. Final inspection generation and exact-HEAD peer signoff are recorded in the local handoff after commitment. No publication is included.
