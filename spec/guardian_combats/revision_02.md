# Guardian inspection revision 2

The combat intent and board answer where danger will land, what ground it creates, and whether a helper is returning. The existing intent rows carry fixed-pattern geometry, surface names, self-Expose and named summons; click/focus inspection supplies exact rules. The map keeps the approved emblems and traces each emblem’s own alpha edge for selection. Primary actions remain card play, movement and route choice. Preserve pointer, keyboard/controller inspection, cancellation and reduced motion. Verify changed intents, actual attack results, surviving hand, all six map emblems and full cutout cycles in native 1920×1080 at 100% UI scale.

The user’s Storm Cantor fixture log reports a freed card proxy in `_take_pooled_card_proxy` during card play and turn draw. The proxy pool must reject duplicate releases and validate a Variant before converting it to Control. Regressions cover both overlapping cleanup and an externally freed entry. Guardian presentation snapshots own board fields only; they never replace the deck or activation state.

Direct bites, strikes and approach attacks track their current target through the ordinary planner. Only explicitly marked ground patterns retain their declared tiles. Pattern aiming evaluates its real geometry, rather than a generic ranged/area approximation. Helpers use complementary safe attack anchors, avoid their Guardian’s declared approach, and advance on every intent. Each Guardian can announce at most one missing roster helper on the next declared activation, with at most two helpers alive and no repeat defeat rewards. Killing a helper after the current declaration buys an activation before its replacement is announced.

Animation acceptance: a recognizable preparation, contact and recovery using the actual weapon, beak or claw, visible at board scale. Idle is only a coordinated body bob with planted supports. No ripple, wave, shimmer, skin crawl or staggered feather/cloth motion. Numerical non-idle samples alone do not prove an attack reads as an attack.

Reinforcement tiles exclude the Guardian’s planned destination and the same intent’s attack area. Otherwise a valid-looking summon could be occupied by its own caster before resolution. The regression cycles missing helpers through five replacements in every encounter, checking the cap and zero repeat rewards. Native playback keeps one RunScene through multiple encounter loads, card plays and turn draws, reproducing the proxy reuse boundary instead of avoiding it with a fresh scene.

Tooltip direction: numbers and keyword effects belong in the action rows. Supplemental enemy/move descriptions contain only encounter-specific rules or exceptions, without repeating the objective, standard status/surface behavior, or normal summon rewards. Expanded move summaries wrap so compact wording never hides a status amount behind an ellipsis.

Descriptions state mechanics, not advice. They do not tell the player how to dodge, which threat to remove, or how standard victory/summon rules work. Named objective chips use the objective title alone. This direction is recorded in the shared UI copy rubric.

A second disappearing-hand regression came from a settled card transition retaining stale hand identities after input unlocked. Its refresh guard now protects staging only while animation owns the hand; unlock always restores the authoritative controls. The regression fails before this change and passes afterward. The native probe accepts the normal disabled-card dimming and checks live container visibility independently.

Retained boards also reused an enemy-ID cutout for a different Guardian on the next encounter. The pool now replaces its renderer when character type changes, and continuous gameplay proof checks the rendered art identity against each live actor.

Fixed-pattern surface notes are restricted to authored ground attacks. Ordinary Ice bites and shots use their normal action rows; they do not claim to leave a missed strike’s entire area behind.
